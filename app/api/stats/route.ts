import {NextResponse} from "next/server";import {db} from "@/lib/db";import {normalizedDistrict} from "@/lib/district";
export const dynamic="force-dynamic";
export async function GET(){const[[counts],[months],[districtRows]]=await Promise.all([db.query<any[]>(`SELECT SUM(role='student') students,SUM(role='school') teachers,COUNT(DISTINCT CASE WHEN role='school' THEN school_name END) schools,(SELECT COUNT(*) FROM resources) resources,(SELECT COUNT(*) FROM resource_activity WHERE action='download') downloads,(SELECT COUNT(*) FROM resource_activity WHERE action='print') prints FROM users WHERE suspended=0`),db.query<any[]>(`SELECT DATE_FORMAT(created_at,'%Y-%m') month,SUM(action='download') downloads,SUM(action='print') prints FROM resource_activity WHERE created_at>=DATE_SUB(CURRENT_DATE,INTERVAL 5 MONTH) GROUP BY month ORDER BY month`),db.query<any[]>(`SELECT district,school_name AS schoolName FROM users WHERE role='school' AND suspended=0 AND district IS NOT NULL`)]);const districtMap=new Map<string,{district:string;accounts:number;schoolNames:Set<string>}>();for(const row of districtRows){const normalized=normalizedDistrict(row.district);if(!normalized.key)continue;const current=districtMap.get(normalized.key)||{district:normalized.label,accounts:0,schoolNames:new Set<string>()};current.accounts+=1;if(row.schoolName)current.schoolNames.add(String(row.schoolName).trim().toUpperCase());districtMap.set(normalized.key,current)}const districtCounts=Array.from(districtMap.values()).map(({district,accounts,schoolNames})=>({district,accounts,schools:schoolNames.size})).sort((a,b)=>a.district.localeCompare(b.district));const c=counts[0]||{};return NextResponse.json({students:Number(c.students||0),teachers:Number(c.teachers||0),districts:districtCounts.length,schools:Number(c.schools||0),resources:Number(c.resources||0),downloads:Number(c.downloads||0),prints:Number(c.prints||0),months,districtCounts,totalUsers:Number(c.students||0)+Number(c.teachers||0)})}
