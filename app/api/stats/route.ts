import {NextResponse} from "next/server";import {db} from "@/lib/db";
export const dynamic="force-dynamic";
export async function GET(){const[[counts],[months],[districts]]=await Promise.all([db.query<any[]>(`SELECT SUM(role='student') students,SUM(role='school') teachers,COUNT(DISTINCT CASE WHEN role='school' THEN district END) districts,COUNT(DISTINCT CASE WHEN role='school' THEN school_name END) schools,(SELECT COUNT(*) FROM resources) resources,(SELECT COUNT(*) FROM resource_activity WHERE action='download') downloads,(SELECT COUNT(*) FROM resource_activity WHERE action='print') prints FROM users WHERE suspended=0`),db.query<any[]>(`SELECT DATE_FORMAT(created_at,'%Y-%m') month,SUM(action='download') downloads,SUM(action='print') prints FROM resource_activity WHERE created_at>=DATE_SUB(CURRENT_DATE,INTERVAL 5 MONTH) GROUP BY month ORDER BY month`),db.query<any[]>(`SELECT district,COUNT(*) accounts,COUNT(DISTINCT school_name) schools FROM users WHERE role='school' AND suspended=0 AND district IS NOT NULL GROUP BY district ORDER BY district`)]);const c=counts[0]||{};return NextResponse.json({students:Number(c.students||0),teachers:Number(c.teachers||0),districts:Number(c.districts||0),schools:Number(c.schools||0),resources:Number(c.resources||0),downloads:Number(c.downloads||0),prints:Number(c.prints||0),months,districtCounts:districts,totalUsers:Number(c.students||0)+Number(c.teachers||0)})}
