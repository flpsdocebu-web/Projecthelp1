import {NextResponse} from "next/server";import {db} from "@/lib/db";import {requireAdministrator,randomUUID} from "@/lib/auth";
export const runtime="nodejs";export const dynamic="force-dynamic";
export async function GET(){const[rows]=await db.query<any[]>(`SELECT id,title,learning_area AS learningArea,grade_level AS gradeLevel,term,file_name AS fileName,file_size AS fileSize,created_at AS createdAt FROM resources ORDER BY learning_area,grade_level,title`);return NextResponse.json({resources:rows})}
export async function POST(request:Request){const admin=await requireAdministrator();if(!admin)return NextResponse.json({error:"Administrator access required."},{status:403});try{const data=await request.formData(),files=data.getAll("files").filter((item):item is File=>item instanceof File),learningArea=String(data.get("learningArea")||""),gradeLevel=String(data.get("gradeLevel")||""),term=String(data.get("term")||"");if(!files.length||!learningArea||!gradeLevel||!term)return NextResponse.json({error:"Subject, grade, term, and PDF files are required."},{status:400});const created=[];for(const file of files){if(file.type!=="application/pdf"&&!file.name.toLowerCase().endsWith(".pdf"))continue;if(file.size>15*1024*1024)return NextResponse.json({error:`${file.name} exceeds the 15 MB database upload limit.`},{status:413});const id=randomUUID(),title=file.name.replace(/\.pdf$/i,""),buffer=Buffer.from(await file.arrayBuffer());await db.execute(`INSERT INTO resources(id,title,learning_area,grade_level,term,file_name,mime_type,file_size,pdf_data,created_by) VALUES(?,?,?,?,?,?,?,?,?,?)`,[id,title,learningArea,gradeLevel,term,file.name,"application/pdf",file.size,buffer,admin.id]);created.push({id,title,learningArea,gradeLevel,term,fileName:file.name})}return NextResponse.json({resources:created},{status:201})}catch{return NextResponse.json({error:"PDF upload failed."},{status:500})}}
