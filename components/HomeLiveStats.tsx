"use client";import {useEffect,useState} from "react";
type Stats={students:number;teachers:number;districts:number;schools:number;resources:number;downloads:number};const empty:Stats={students:0,teachers:0,districts:0,schools:0,resources:0,downloads:0};
function AnimatedNumber({value,replay}:{value:number;replay:number}){const[display,setDisplay]=useState(0);useEffect(()=>{let frame=0;const start=performance.now(),animate=(time:number)=>{const p=Math.min(1,(time-start)/4000);setDisplay(Math.round(value*(1-Math.pow(1-p,3))));if(p<1)frame=requestAnimationFrame(animate)};frame=requestAnimationFrame(animate);return()=>cancelAnimationFrame(frame)},[value,replay]);return <>{display.toLocaleString()}</>}
export default function HomeLiveStats({location}:{location:"hero"|"numbers"}){const[data,setData]=useState<Stats>(empty),[replay,setReplay]=useState(0);useEffect(()=>{const load=()=>fetch("/api/stats",{cache:"no-store"}).then(r=>r.json()).then(setData).catch(()=>{});load();const timer=setInterval(load,180000);return()=>clearInterval(timer)},[]);const animate=()=>setReplay(v=>v+1);if(location==="hero")return <div className="floating-stat top interactive-live-stat" role="button" tabIndex={0} onMouseEnter={animate} onClick={animate}><b><AnimatedNumber value={data.downloads} replay={replay}/></b><span>Resources downloaded</span></div>;const stats=[{value:data.students,label:"Active student users",icon:"♙"},{value:data.teachers,label:"Teacher/School users",icon:"♟"},{value:data.districts,label:"Districts registered",icon:"●"},{value:data.schools,label:"Schools registered",icon:"⌂"},{value:data.resources,label:"Learning Activity Sheets uploaded",icon:"▤"}];return <section className="numbers live-home-numbers">{stats.map(s=><button key={s.label} onMouseEnter={animate} onClick={animate}><i>{s.icon}</i><strong><AnimatedNumber value={s.value} replay={replay}/></strong><span>{s.label}</span><small>Live database count</small></button>)}</section>}
