"use client";
import {useEffect,useState} from "react";
export default function AdminGuard({children}:{children:React.ReactNode}){const[authorized,setAuthorized]=useState(false);useEffect(()=>{fetch("/api/auth/session",{cache:"no-store"}).then(r=>r.json()).then(({user})=>{if(user?.role==="administrator")setAuthorized(true);else window.location.replace("/login/")}).catch(()=>window.location.replace("/login/"))},[]);if(!authorized)return <main className="admin-loading"><div className="admin-logo-loader"><span/><img src="/project-helps-logo.png" alt="Project HELPS loading"/></div><p>Opening administrator dashboard…</p></main>;return <>{children}</>}
